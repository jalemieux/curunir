services:
  curunir:
    build: .
    env_file: .env
    ports:
      - "8765:8765"
    volumes:
      - ./secrets:/secrets:ro
      - ./workspace:/app/workspace
      - ./context:/app/context
    restart: unless-stopped
