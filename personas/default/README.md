# Default Persona

The fallback persona loaded when `CURUNIR_PERSONA` is unset. Ships the full
skill catalog and the baseline behavior prompt (`prompts/behavior.md`).

To run a different persona, set `CURUNIR_PERSONA=<name>` and add a bundle at
`personas/<name>/`.
